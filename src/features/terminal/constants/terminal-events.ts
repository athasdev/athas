export const CLOSE_TERMINAL_EVENT = "terminal-close";
export const RENAME_TERMINAL_EVENT = "terminal-rename";
/** Dispatched on `window` when a terminal's process ends, with its exit status. */
export const TERMINAL_PROCESS_EXIT_EVENT = "terminal-process-exit";
