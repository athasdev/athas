/**
 * Layout hook stub for backward compatibility
 */

export function useLayout() {
  return {
    gutterWidth: 60,
    charWidth: 8.4,
    lineHeight: 20,
  };
}

export function useEditorLayout() {
  return {
    gutterWidth: 60,
    charWidth: 8.4,
    lineHeight: 20,
  };
}
