---
title: macOS Setup
description: Set up development environment on macOS
---

## Prerequisites

- Xcode Command Line Tools
- Rust (via rustup)
- Node.js 18+ and bun

## Install Dependencies

```bash
# Install Xcode CLI tools
xcode-select --install

# Install Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Install bun
curl -fsSL https://bun.sh/install | bash
```

## Clone and Build

```bash
git clone https://github.com/athasdev/athas.git
cd athas
bun install
bun dev
```

## Troubleshooting

If you encounter issues with Tauri, ensure you have the required system dependencies:

```bash
brew install pkg-config
```
